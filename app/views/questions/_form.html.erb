<h1><%= question_header(@question) %></h2>

<%= form_with model: [:admin, @test, @question], local: true do |f| %>
<%= render 'shared/errors', resource: @question %>

  <p>
    <%= f.label :body %>
    <%= f.text_field :body, id: :question_body %>
  </p><br>
   
  <%= f.submit %>