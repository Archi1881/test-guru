<h1><%= @question.test.title %>
<p><%= @question.body %></p>

<h2>Answers</h2>
<%= link_to 'Add answer', new_test_question_path(@question) %>

<%= link_to 'Edit question', edit_question_path(@question) %>

<%= link_to 'Back', test_path(@question.test) %>